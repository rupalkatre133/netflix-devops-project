apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: netflix-ingress

spec:

  rules:

  - http:

      paths:

      - path: /

        pathType: Prefix

        backend:

          service:

            name: netflix-service

            port:

              number: 80
